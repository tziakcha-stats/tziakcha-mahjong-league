import { AdminPlaceholder } from "@/features/admin-shell/components/admin-placeholder";

export default function AdminPage() {
  return (
    <AdminPlaceholder
      eyebrow="控制台"
      title="后台首页占位"
      description="这里将来可以承载待办事项、赛事状态、审核提醒和数据总览。当前版本主要用于固定管理区域的布局风格。"
    />
  );
}
